
package crpgs;

import java.util.Collection;
import java.util.Iterator;
import java.util.Set;

/**
 *
 * @author Vaize
 */
public class Main {
    public static void main(String[] args) {
        RPSystem rps = new RPSystem();
        
        rps.addElement(new Element("1-1","1"));
        rps.addElement(new Element("2-1","2"));
        rps.addElement(new Element("1-2","1"));
        rps.addElement(new Element("2-2","2"));
        rps.removeElement("2", "2-2");
        
        Set types = rps.getTypes();
        Iterator<String> iterator = types.iterator();
        while(iterator.hasNext()) {
            Set names = rps.getNamesByType(iterator.next());
            Iterator<String> iterator2 = names.iterator();
            while(iterator2.hasNext()) {
                System.out.println(iterator2.next());
            }
        }
        
        Collection elements = rps.getElementsByType("1");
        Iterator<Element> iterator2 = elements.iterator();
        while(iterator2.hasNext()) {
            Element e = iterator2.next();
            
        }
    }
}