<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="com.sam.team.character.viewmodel.RPSystem"/>
        <variable
            name="system"
            type="RPSystem" />
        <variable
            name="cardclick"
            type="android.view.View.OnClickListener" />
        <variable
            name="addclick"
            type="android.view.View.OnClickListener" />
        <variable
            name="shareclick"
            type="android.view.View.OnClickListener" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/system_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:textColor="@color/colorPrimaryText"
            android:textSize="@dimen/text_display1_type"
            android:text="@{system.name + ' ' + system.version}"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:orientation="horizontal"
            android:layout_gravity="end">

            <ImageButton
                android:id="@+id/btn_share"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@android:drawable/ic_menu_share"
                android:contentDescription="@string/system_item_share_icon"
                android:onClick="@{shareclick}"/>

            <ImageButton
                android:id="@+id/btn_add_element"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@android:drawable/ic_menu_add"
                android:contentDescription="@string/system_item_add_icon"
                android:onClick="@{addclick}"/>
        </LinearLayout>

    </RelativeLayout>
</layout>